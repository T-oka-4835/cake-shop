
  <div>
    <%= attachment_image_tag @item, :image, :fill, 100, 100, format: 'jpeg', class: "item-picture", fallback: "no_image.jpg" %>
  </div>
  <div>
    <%= @item.explanation %>
  </div>
  <div>
    ¥<%= @item.price %>(税込)
  </div>

  <div>
    カートに入れる
  </div>