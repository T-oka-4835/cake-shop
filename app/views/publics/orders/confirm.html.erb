<h4>注文情報確認</h4>
<table>
  <tr>
    <th>商品名</th>
    <th>単価(税込)</th>
    <th>数量</th>
    <th>小計</th>
  </tr>
  <tbody>
    <% @carts.each do |cart| %>
      <tr>
        <td>
          <%= cart.item.name %>
        </td>
        <td>
          <%= cart.item.price %>
        </td>
        <td>
          <!--<= cart.item.amount %>-->
        </td>
         <td>
          <!--<= cart.item.price * cart.amount.to_i %>-->
        </td>
        <td>
          <%= link_to "削除する", cart_path(cart), method: :delete %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>